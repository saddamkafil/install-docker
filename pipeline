pipeline
{
    agent{
    label 'node1'}
    triggers 
    {
    pollSCM '* * * * *'
    }
    stages 
    {
        stage('install docker')
        {
            steps
            {
                sh 'sudo yum install docker -y'
                sh 'sudo docker --version'
            }
        }
    }
}
